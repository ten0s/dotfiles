---
- hosts: localhost
  connection: local
  tasks:
  - name: Install SML deps
    sudo: yes
    apt: name={{item}} state=latest
    with_items:
    - libsmlnj-smlnj
    - mlton-compiler
    - rlwrap
    - smlnj
    - sml-mode
