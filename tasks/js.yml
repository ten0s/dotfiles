---
#
# https://deb.nodesource.com/setup_5.x
#
- name: Add NodeSource signing key
  become: true
  apt_key: url=https://deb.nodesource.com/gpgkey/nodesource.gpg.key
           state=present

- name: Add NodeSource Node.js 5.x repo
  become: true
  apt_repository: repo="{{item}}"
                  state=present
  with_items:
  - "deb https://deb.nodesource.com/node_5.x trusty main"
  - "deb-src https://deb.nodesource.com/node_5.x trusty main"

- name: Install Node.js
  become: true
  apt: name=nodejs state=present

- name: Install global Node's packages
  become: true
  npm: name={{item.name}}
       version={{item.version}}
       global=true
       state=present
  with_items:
  - {name: "bower", version: "1.7.9"}
  - {name: "elasticdump", version: "3.0.0"}
  - {name: "jslint", version: "0.9.6"}
