source ~/.kerl/installations/{{erl_ver}}/activate
# FIX automatically set MANPATH
unset MANPATH

# FIX: ~/.kerl/installations/{{erl_ver}}/bin/start takes precedence over /sbin/start
export PATH=/sbin:$PATH
export ERL_AFLAGS="-kernel shell_history enabled"

alias cdv="$_KERL_ACTIVE_DIR/lib/observer-*/priv/bin/cdv"
