---
- name: Install deps
  become: true
  apt:
    name:
    - build-essential
    - libxml2-dev
    - libxslt-dev
    - python-dev
    state: latest

- name: Upgrade pip
  become: true
  pip:
    name: pip
    state: latest

- name: Install virtualenv
  pip:
    name: virtualenv
    extra_args: --user
    state: latest

- name: Install ansible-lint
  pip:
    name: ansible-lint
    extra_args: --user
    state: latest
